export PYENV_ROOT="$HOME/.pyenv"
export AWS_PROFILE=nebula-prod-sso
export EDITOR=nvim
export VISUAL=nvim
export HOMEBREW_NO_ENV_HINTS=1
export TERM=xterm
export PIPENV_VENV_IN_PROJECT=1 # pipenv will create a virtualenv in the project directory similar to node_modules

